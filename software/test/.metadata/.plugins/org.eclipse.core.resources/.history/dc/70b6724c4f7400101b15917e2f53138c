#include "motor.h"

/* MOTOR RUN */
void motor_run(struct motor *self){

	/* Set starting duty cycle */
	self->_dutyCycle = 0;

	/* Enable PWM */
	RCC->AHB1ENR |= RCC_AHB1ENR_GPIODEN;

	/* Configure GPIOD port number as general purpose output */
	GPIOD->MODER &= ~GPIOD_MODER_MODER12

	/* Set enable pin */
}

