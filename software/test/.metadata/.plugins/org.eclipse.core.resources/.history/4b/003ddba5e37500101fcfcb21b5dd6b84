#include "control_pin.h"

void control_pin_init(struct control_pin *self, GPIO_TypeDef *port, uint8_t pin){

	_port = port;
	_pin = pin;

	/* Set GPIO as general purpose output */
	self->_port->MODER &= (0x3UL << 2 * _pin);
}

